public static String reverseWords(String str){
 // solution here 
 
 List<String> ans = s.split(' ');
 Integer n = ans.length();
 Integer right =n-1;
 Integer left = 0;

 while(left<right){
     
     if(ans[left] == ' '){
         left++;
     } else if(ans[right] == ' '){
         right--;
     }else {
         
         String temp =  ans[left];
         ans[left] = ans[right];
         ans[right]= temp;

         left++;
         right--;

     } 
 }
 return String.join(ans,' ');
}